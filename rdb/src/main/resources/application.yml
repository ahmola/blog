server:
  port: 8081
spring:
  application:
    name: rdb
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username: "root"
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show:
          sql: false
        format_sql: false